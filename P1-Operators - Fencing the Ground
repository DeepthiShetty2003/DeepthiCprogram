#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int l,b ;
    scanf("%d", &l);
     scanf("%d", &b);   
    int peri = 2*(l+b);
    int area = (l*b);
    printf("The required length is %d m\n",peri );
            printf("The required area of carpet is %d sqm\n", area);
    
    return 0;
}
