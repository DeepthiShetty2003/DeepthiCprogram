#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int x, y;
    scanf("%d", &x);
    scanf("%d", &y);
    int X = (x-3)*(x-3);
    int Y =(y-4)*(y-4);
    int dist = sqrt(X+Y);
    printf("%d", dist);
    
    return 0;
}
